<UserControl x:Class="Gui.Views.KeybindingEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300"
             xmlns:vm="clr-namespace:Gui.ViewModels"
             d:DataContext="{d:DesignInstance vm:KeybindingViewModel}" x:ClassModifier="internal">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition Width="15"/>
        </Grid.ColumnDefinitions>
        <WrapPanel Grid.Column="0">
            <Label Content="{Binding KeybindingName}" />
            <Label Content=":" />
        </WrapPanel>
        
        <Button Content="{Binding Keyname}" Command="{Binding ChangeCommand}"  Grid.Column="1"/>
        <Rectangle x:Name="StatusRect" Width="10" Height="10" Grid.Column="2"></Rectangle>
    </Grid>
</UserControl>
